<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/head' %>
    <%= render 'layouts/shim' %>
  </head>
  <body class="fo" style="background: linear-gradient(to bottom, #dcdcdc, #6b8e23, #dcdcdc);">
        <div class="fade_group">
        <div></div>
        <div></div>
        <div></div>
        <div></div>
    </div>
    <div class="fade_group2">
        <div></div>
        <div></div>
     </div> 
    <div class="container top-indent">
    <%= render 'layouts/header' %>
    <div class="container">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
      <%= render 'layouts/footer' %>
      <br><br><br>
      <%= debug(params) if Rails.env.development? %>
  </body>
</html>